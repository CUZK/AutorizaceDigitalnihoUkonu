@{
    ViewBag.Title = "Výsledek ověření kódu";
}

<h1>@ViewBag.Title</h1>

<div class="row">
    <div class="col-md-12">
        @if (!ViewData.ModelState.IsValid)
        {
            <h2 class="error">Chyba (viz NIA ResponseBodyXML)</h2>
            @Html.ValidationSummary()
        }
        <br />
        @Html.Partial("_DevOutputs")
    </div>
</div>